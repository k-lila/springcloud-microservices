server:
  port: 8081

spring:
  application:
    name: client-service
  data:
    mongodb:
      uri: mongodb://mongo-database:27017/client_db
      auto-index-creation: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://discovery-server:9091/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

application-version: 1.0
